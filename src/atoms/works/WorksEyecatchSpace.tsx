import styled from 'styled-components';

const WorksEyecatchSpace = () => {
    const rand = 0;
    console.log(rand);

    return(
        <WorksEyecatchSpaceComponent>
        </WorksEyecatchSpaceComponent>
    );
};

const WorksEyecatchSpaceComponent = styled.div`
    width: 300px;
`; 

export default WorksEyecatchSpace;